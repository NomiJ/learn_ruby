
-unless @doc.blank?
	- @docs.each do |doc|
		%a{href: (url_for [doc])}
			.doc
			%h2.title= link_to doc.title, doc
			%p.date = time_ago_in_words(doc.created_at)
			%p.content= truncate(doc.content, length: 50)

-else 
	%h2 Creat Doc!
	%p Start Creating Doc
	%button= link_to "Create Doc", new_doc_path

