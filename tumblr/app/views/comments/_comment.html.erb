
<em><%= comment.name %></em>
<i><%= comment.body %></i>
<u><%= time_ago_in_words(comment.created_at)%></u>

	<% if  user_signed_in? %>

<%= link_to 'Delete Comment', [comment.post, comment], method: :delete, class: "button", data: {
    confirm: 'Are you sure?'
}%>

<%end%>