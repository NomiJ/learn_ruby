<%= form_for([project, project.tasks.build]) do |f| %>
 
  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content, placeholder: 'New Task' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
